log(-1)
##está mal, pero el programa sigue corriendo 
imprimeMSJ<-function(x){
    if(x>0)
}

#Traceback
mean(x) #0arca err6r porque no hay ningun objeto x
traceback()
ln(y~x)
lm(y~x)
traceback()
debug(lm) #hace una dinamica de tal manera que corra renglón por renglón,
          #como en vba, cada que se va corriendo en la consola se coloca una n
lm(y~x)
options(error = recover)
?Error
?options
selecion:0 #para salirte
